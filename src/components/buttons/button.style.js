import { colors } from '../../constants/theme.js';
export const styles = {

    btn: {
        width: '100%',
        backgroundColor: colors.green,
        padding: 12,
        borderRadius: 6,
        cursor: 'pointer',
        outline: 'none'
    },

    text: {
        color: 'white',
        fontSize: 18,
        fontWeight: 'bold',
        textAlign: 'center'
    }
}